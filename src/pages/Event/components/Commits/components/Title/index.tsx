import React from 'react';
import {Text} from 'react-native';
import styles from './styles';

const Title: React.FC = () => {
    return <Text style={styles.title}>Commits:</Text>;
};

export default Title;
